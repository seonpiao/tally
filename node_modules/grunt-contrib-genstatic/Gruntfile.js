module.exports = function(grunt) {

    grunt.initConfig({
        pkg: grunt.file.readJSON('package.json'),
        genstatic: {
            testapp: {
                file: 'tests/test.jade',
                modulePath: 'tests/modules',
                prefix: {
                    js: 'module/'
                },
                dest: {
                    js: 'tests/module.js'
                }
            }
        }
    });

    grunt.loadTasks('tasks');

    grunt.registerTask('default', ['genstatic']);

};